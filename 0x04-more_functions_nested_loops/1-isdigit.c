#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _isdigit - Called from main
 * @c: Integer declared
 * Description: Detects digits 0 to 9
 * Return: 1 for digit
 */
int _isdigit(int c)
{
	if (c = 0 && c < 10)
		return (1);
	return (0);
}

